import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:info_app/app/modules/status/status_module.dart';

void main() {
  initModule(StatusModule());
  // BuildProfileImageController buildprofileimage;
  //
  setUp(() {
    //     buildprofileimage = StatusModule.to.get<BuildProfileImageController>();
  });

  group('BuildProfileImageController Test', () {
    //   test("First Test", () {
    //     expect(buildprofileimage, isInstanceOf<BuildProfileImageController>());
    //   });

    //   test("Set Value", () {
    //     expect(buildprofileimage.value, equals(0));
    //     buildprofileimage.increment();
    //     expect(buildprofileimage.value, equals(1));
    //   });
  });
}
