import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:info_app/app/modules/tabb/tabb_module.dart';

void main() {
  initModule(TabbModule());
  // TabController tab;
  //
  setUp(() {
    //     tab = TabModule.to.get<TabController>();
  });

  group('TabbController Test', () {
    //   test("First Test", () {
    //     expect(tab, isInstanceOf<TabController>());
    //   });

    //   test("Set Value", () {
    //     expect(tab.value, equals(0));
    //     tab.increment();
    //     expect(tab.value, equals(1));
    //   });
  });
}
