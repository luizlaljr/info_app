import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('TabbPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TabbPage(title: 'Tabb')));
    //  final titleFinder = find.text('Tabb');
    //  expect(titleFinder, findsOneWidget);
  });
}
