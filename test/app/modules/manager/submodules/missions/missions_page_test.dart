import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:info_app/app/modules/manager/submodules/missions/missions_page.dart';

main() {
  testWidgets('MissionsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MissionsPage(title: 'Missions')));
    //  final titleFinder = find.text('Missions');
    //  expect(titleFinder, findsOneWidget);
  });
}
