import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:info_app/app/modules/manager/submodules/missions/missions_controller.dart';
import 'package:info_app/app/modules/manager/submodules/missions/missions_module.dart';

void main() {
  initModule(MissionsModule());
  // MissionsController missions;
  //
  setUp(() {
    //     missions = MissionsModule.to.get<MissionsController>();
  });

  group('MissionsController Test', () {
    //   test("First Test", () {
    //     expect(missions, isInstanceOf<MissionsController>());
    //   });

    //   test("Set Value", () {
    //     expect(missions.value, equals(0));
    //     missions.increment();
    //     expect(missions.value, equals(1));
    //   });
  });
}
