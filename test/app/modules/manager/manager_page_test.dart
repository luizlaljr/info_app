import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:info_app/app/modules/manager/manager_page.dart';

main() {
  testWidgets('ManagerPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ManagerPage(title: 'Manager')));
    //  final titleFinder = find.text('Manager');
    //  expect(titleFinder, findsOneWidget);
  });
}
