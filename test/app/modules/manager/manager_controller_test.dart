import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:info_app/app/modules/manager/manager_controller.dart';
import 'package:info_app/app/modules/manager/manager_module.dart';

void main() {
  initModule(ManagerModule());
  // ManagerController manager;
  //
  setUp(() {
    //     manager = ManagerModule.to.get<ManagerController>();
  });

  group('ManagerController Test', () {
    //   test("First Test", () {
    //     expect(manager, isInstanceOf<ManagerController>());
    //   });

    //   test("Set Value", () {
    //     expect(manager.value, equals(0));
    //     manager.increment();
    //     expect(manager.value, equals(1));
    //   });
  });
}
